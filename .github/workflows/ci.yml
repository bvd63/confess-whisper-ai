name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_call:
    secrets:
      VITE_SUPABASE_URL:
        required: false
      VITE_SUPABASE_PUBLISHABLE_KEY:
        required: false

env:
  NODE_VERSION: '20'

jobs:
  build-test-analyze:
    name: Build, Test & Analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Export Supabase env
        shell: bash
        run: |
          set -euo pipefail

          choose_value() {
            local secret="$1"
            local variable="$2"
            if [[ -n "$secret" ]]; then
              printf '%s' "$secret"
            elif [[ -n "$variable" ]]; then
              printf '%s' "$variable"
            fi
          }

          VITE_SUPABASE_URL="$(choose_value "${{ secrets.VITE_SUPABASE_URL }}" "${{ vars.VITE_SUPABASE_URL }}")"
          if [[ -z "$VITE_SUPABASE_URL" ]]; then
            echo "VITE_SUPABASE_URL is required for CI" >&2
            exit 1
          fi
          echo "::add-mask::$VITE_SUPABASE_URL"
          echo "VITE_SUPABASE_URL=$VITE_SUPABASE_URL" >> "$GITHUB_ENV"

          VITE_SUPABASE_PUBLISHABLE_KEY="$(choose_value "${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}" "${{ vars.VITE_SUPABASE_PUBLISHABLE_KEY }}")"
          if [[ -z "$VITE_SUPABASE_PUBLISHABLE_KEY" ]]; then
            echo "VITE_SUPABASE_PUBLISHABLE_KEY is required for CI" >&2
            exit 1
          fi
          echo "::add-mask::$VITE_SUPABASE_PUBLISHABLE_KEY"
          echo "VITE_SUPABASE_PUBLISHABLE_KEY=$VITE_SUPABASE_PUBLISHABLE_KEY" >> "$GITHUB_ENV"

      - name: Type Check
        run: npx tsc --noEmit

      - name: Lint & Auto-Fix
        run: npm run lint -- --fix || true

      - name: ENV Validation
        run: npm run check:env

      - name: Unit Tests
        run: npm run test:unit

      - name: Integration Tests
        run: npm run test:integration

      - name: E2E Tests
        run: npm run test:e2e

      - name: Coverage Report
        run: npm run coverage

      - name: Build (production)
        run: npm run build

      - name: Admin Bundle Guard
        run: npm run guard:admin-chunk
        env:
          ADMIN_CHUNK_LIMIT_KB: 500

      - name: Bundle Size Report
        run: npm run analyze || true

      - name: Save reports
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            coverage
            dist

      - name: Lighthouse Performance (mobile)
        run: npx lhci autorun --collect.settings.preset=mobile || true

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: build-test-analyze
    if: github.ref == 'refs/heads/develop'
    environment:
      name: staging
      url: https://staging.confessai.app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: reports
          path: dist/
      - name: Deploy to staging
        run: |
          echo "Deploying to staging environment"
          # Add your staging deployment commands here
      - name: Health check
        run: |
          sleep 30
          curl -f https://staging.confessai.app/api/health || exit 1

  deploy-canary:
    name: Deploy Canary (10%)
    runs-on: ubuntu-latest
    needs: build-test-analyze
    if: github.ref == 'refs/heads/main'
    environment:
      name: production-canary
      url: https://confessai.app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: reports
          path: dist/
      - name: Deploy canary
        run: |
          echo "Deploying canary (10% traffic)"
          # Add canary deployment commands
      - name: Health check
        run: |
          sleep 30
          curl -f https://confessai.app/api/health || exit 1
      - name: Monitor metrics
        run: |
          sleep 300  # Monitor for 5 minutes
          # Add metrics validation

  deploy-production:
    name: Deploy to Production (100%)
    runs-on: ubuntu-latest
    needs: deploy-canary
    if: github.ref == 'refs/heads/main'
    environment:
      name: production
      url: https://confessai.app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: reports
          path: dist/
      - name: Deploy to production
        run: |
          echo "Deploying to production (100% traffic)"
          # Add production deployment commands
      - name: Final health check
        run: |
          sleep 30
          curl -f https://confessai.app/api/health || exit 1
          curl -f https://confessai.app/api/metrics || exit 1

  rollback:
    name: Rollback on Failure
    runs-on: ubuntu-latest
    needs: [deploy-canary, deploy-production]
    if: failure()
    steps:
      - name: Rollback deployment
        run: |
          echo "Rolling back deployment"
          # Add rollback commands here
